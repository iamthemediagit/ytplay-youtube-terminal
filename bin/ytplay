#!/bin/bash
# ytplay - Recherche YouTube interactive avec sélection fzf + lecture mpv
# Version: 2.1
# Usage: ytplay "search query" [max_results]

set -e

QUERY="${1:-documentaire inédit}"
MAX_RESULTS="${2:-20}"

# Vérifier les dépendances
for cmd in yt-dlp fzf jq mpv; do
    if ! command -v $cmd &> /dev/null; then
        echo "❌ Erreur: $cmd n'est pas installé"
        exit 1
    fi
done

# Logo ASCII
cat << 'EOF'
 ┌─────────────────────────────────────────┐
 │  ▀▄    ▄  ▀▀█▀▀  █▀▀▄  █     █▀▀█  ▀▄    ▄ │
 │   █  █     █    █▄▄█  █     █▄▄█   █  █  │
 │    ▀▀      ▀    ▀     ▀▀▀   ▀  ▀    ▀▀   │
 │  YouTube Terminal Player v2.1           │
 └─────────────────────────────────────────┘
EOF
echo ""
echo "🔍 Recherche: \"$QUERY\" ($MAX_RESULTS résultats)..."
echo ""

# Extraire les résultats YouTube en JSON (mode rapide)
RESULTS=$(yt-dlp --dump-json --flat-playlist --skip-download "ytsearch${MAX_RESULTS}:${QUERY}" 2>/dev/null)

if [ -z "$RESULTS" ]; then
    echo "❌ Aucun résultat trouvé"
    exit 1
fi

# Formater pour fzf: "Titre | Durée | Vues | Auteur | ID"
FORMATTED=$(echo "$RESULTS" | jq -r '
    .title[0:60] + "|" + 
    (if .duration then (.duration | tostring) else "0" end) + "|" + 
    (if .view_count then (.view_count | tostring) else "0" end) + "|" + 
    (.uploader // "Unknown")[0:25] + "|" + 
    .id
' | while IFS='|' read -r title duration views uploader id; do
    # Formater la durée
    duration=$(echo "$duration" | tr -d ' ')
    if [ "$duration" = "0" ] || [ -z "$duration" ]; then
        dur_formatted="N/A"
    else
        hours=$((duration / 3600))
        minutes=$(((duration % 3600) / 60))
        if [ $hours -gt 0 ]; then
            dur_formatted="${hours}h$(printf '%02d' $minutes)m"
        else
            dur_formatted="${minutes}min"
        fi
    fi
    
    # Formater les vues
    views_num=$(echo "$views" | tr -d ' ')
    if [ "$views_num" -gt 1000000 ] 2>/dev/null; then
        views_formatted="$(awk "BEGIN {printf \"%.1f\", $views_num/1000000}")M"
    elif [ "$views_num" -gt 1000 ] 2>/dev/null; then
        views_formatted="$((views_num / 1000))K"
    else
        views_formatted="$views_num"
    fi
    
    # Nettoyer l'ID et créer le lien complet
    id_clean=$(echo "$id" | tr -d ' ')
    video_link="https://youtu.be/$id_clean"
    
    # Format: Titre | Durée | Vues | Auteur | Lien
    printf "%-60s | %7s | %6s | %-25s | %s\n" \
        "$(echo "$title" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//')" \
        "$dur_formatted" \
        "$views_formatted" \
        "$(echo "$uploader" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//')" \
        "$video_link"
done)

# Header pour fzf
HEADER="Titre                                                        | Durée   | Vues   | Auteur                    | Lien (Cmd+Click)"

# Sélection interactive avec fzf (multi-sélection avec Tab)
SELECTED=$(echo "$FORMATTED" | fzf \
    --multi \
    --prompt="🎵 Sélection ▶ " \
    --height=100% \
    --layout=reverse \
    --info=inline \
    --border=rounded \
    --header="$HEADER" \
    --header-lines=0 \
    --preview-window=up:3:wrap \
    --preview='echo "📺 Sélectionné: {1}"' \
    --bind='ctrl-a:select-all,ctrl-d:deselect-all' \
    --color='header:italic:underline')

if [ -z "$SELECTED" ]; then
    echo "❌ Aucune sélection"
    exit 0
fi

# Compter le nombre de sélections
NUM_SELECTED=$(echo "$SELECTED" | wc -l | tr -d ' ')

# Extraire les URLs
VIDEO_URLS=()
while IFS= read -r line; do
    # Extraire l'ID depuis le lien (format: 🔗 VIDEO_ID)
    VIDEO_ID=$(echo "$line" | awk -F'|' '{print $NF}' | grep -oE '[a-zA-Z0-9_-]{11}' | head -1)
    if [ -n "$VIDEO_ID" ]; then
        VIDEO_URLS+=("https://www.youtube.com/watch?v=${VIDEO_ID}")
    fi
done <<< "$SELECTED"

echo ""
if [ "$NUM_SELECTED" -eq 1 ]; then
    VIDEO_TITLE=$(echo "$SELECTED" | awk -F' \\| ' '{print $1}')
    echo "▶️  Lecture: $VIDEO_TITLE"
else
    echo "▶️  Lecture de $NUM_SELECTED vidéos (playlist)"
    echo "   Utilisez < et > dans mpv pour naviguer"
fi
echo ""

# Demander audio ou vidéo
echo "Mode de lecture:"
echo "  [1] Audio seul (défaut)"
echo "  [2] Vidéo"
echo -n "Choix (1/2): "
read -t 5 MODE || MODE="1"
echo ""

# Afficher les raccourcis clavier
echo "⌨️  Raccourcis mpv:"
echo "   <  >     : Vidéo précédente/suivante"
echo "   ←  →     : Reculer/Avancer 5s"
echo "   ↑  ↓     : Volume +/-"
echo "   Space    : Pause/Play"
echo "   q        : Quitter"
echo ""

case $MODE in
    2)
        echo "📹 Lecture vidéo..."
        echo ""
        # Options mpv avec compteur playlist
        mpv --term-status-msg='Playlist: [${playlist-pos-1}/${playlist-count}] ⏱ ${time-pos}/${duration} | ${media-title}' \
            --term-playing-msg='▶️  Lecture [${playlist-pos-1}/${playlist-count}]: ${media-title}' \
            "${VIDEO_URLS[@]}"
        ;;
    *)
        echo "🎧 Lecture audio..."
        echo ""
        # Options mpv audio avec compteur playlist
        mpv --no-video \
            --term-status-msg='♪ Playlist: [${playlist-pos-1}/${playlist-count}] | ⏱ ${time-pos}/${duration} | ${media-title}' \
            --term-playing-msg='▶️  Audio [${playlist-pos-1}/${playlist-count}]: ${media-title}' \
            "${VIDEO_URLS[@]}"
        ;;
esac
